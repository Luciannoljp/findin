# FINDIN 🔍

Sistema de busca e ranking de produtos com cache local, desenvolvido em Python utilizando Flask.

## 🚀 Funcionalidades
- Busca inteligente
- Cache local em JSON
- Normalização e ranking de resultados
- Interface web simples

## 🛠 Tecnologias
- Python
- Flask
- JavaScript
- HTML/CSS

## 📂 Estrutura
- `app.py` → aplicação principal
- `search_engine.py` → motor de busca
- `ranker.py` → ranking de resultados
- `cache/` → cache local (não versionado)
- `static/` → arquivos JS
- `templates/` → HTML

## ▶️ Como executar

```bash
git clone https://github.com/SEU_USUARIO/FINDIN.git
cd FINDIN
pip install -r requirements.txt
python app.py